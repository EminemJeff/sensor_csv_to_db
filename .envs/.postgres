# PostgreSQL
# ------------------------------------------------------------------------------
POSTGRES_HOST=sensor_database
POSTGRES_PORT=5432
POSTGRES_DB=Sensors_DB
POSTGRES_USER=root
POSTGRES_PASSWORD=root
# export DATABASE_URL="postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"